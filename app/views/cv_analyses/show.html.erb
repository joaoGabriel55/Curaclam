<%= turbo_stream_from "cv_analysis_#{@cv_analysis.id}" %>

<div class="container">
  <header class="page-header">
    <h1>CV Analysis Dashboard</h1>
    <%= link_to "← Back to All Analyses", cv_analyses_path, class: "btn btn-secondary" %>
  </header>

  <% if notice.present? %>
    <div class="alert alert-success">
      <%= notice %>
    </div>
  <% end %>

  <%= render "cv_analyses/analysis_content", cv_analysis: @cv_analysis %>
</div>
